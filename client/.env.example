# Origine de l'API.
# En developpement, laissez vide : Vite relaie /api vers http://localhost:4000
# (voir vite.config.js), ce qui garde le front et l'API sur la meme origine.
# En production, indiquez l'URL complete de l'API AVANT de lancer `npm run build`.
VITE_API_URL=
